<h2 class="subhead navbar text-center alert"><%= t(:details) %></h2>

<div class="scheduler-border">

    <p>
      <strong><%= t(:name)%>:</strong>
      <%= @category.name %>
    </p>

</div>

<%= link_to t(:edit), edit_category_path(@category), class: 'btn btn-success' %>
<%= link_to t(:back), categories_path, class: 'btn btn-default back2' %>
<br><br><br>