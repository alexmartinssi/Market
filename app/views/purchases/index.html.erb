<h2 class="subhead navbar text-center alert">Compras</h2>

<table class="table">
  <thead>
    <tr>
      <!--<th>Item</th>-->
      <th class="text-center">Código</th>
      <th class="text-center">Fornecedor</th>
      <!--<th class="text-center">Valor da Compra</th>-->
      <th class="text-center">Data da compra</th>
      <th colspan="3" class="text-center"><%= t(:action)%></th>
    </tr>
  </thead>

  <tbody class="text-center">
    <% @purchases.each do |purchase| %>
      <tr>
        <td><%= purchase.id %></td>
        <td><%= purchase.supplier.name %></td>
        <!--<td><%= purchase.total %></td>-->
        <td><%= purchase.created_at.strftime("%d/%m/%Y")%></td>
        <td><%= link_to '', purchase, class: 'btn btn-default glyphicon glyphicon-th-list' %></td>
        <td><%= link_to '', edit_purchase_path(purchase), class: 'btn btn-default glyphicon glyphicon-edit' %></td>
        <td><%= link_to '', purchase, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-default glyphicon glyphicon-trash'%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Compra', new_purchase_path,class: 'btn btn-primary' %>
<%= link_to t(:back), root_path, class: 'btn btn-default back2' %>
