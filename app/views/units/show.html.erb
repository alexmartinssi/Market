<h2 class="subhead navbar text-center alert"><%= t(:details) %></h2>

<div class="scheduler-border">

    <p>
      <strong><%= t(:description)%>:</strong>
      <%= @unit.description %>
    </p>

    <p>
      <strong><%= t(:acronym)%>:</strong>
      <%= @unit.acronym %>
    </p>

</div>

<%= link_to t(:edit), edit_unit_path(@unit), class: 'btn btn-success' %>
<%= link_to t(:back), units_path, class: 'btn btn-default back2' %>
<br><br><br>